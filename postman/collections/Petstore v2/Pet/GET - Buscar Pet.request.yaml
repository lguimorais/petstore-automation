$kind: http-request
name: GET - Buscar Pet
url: "{{base_url}}/pet/{{pet_id}}"
method: GET
body:
  type: json
  content: ""
scripts:
  - type: afterResponse
    code: |
      pm.test("Status 200", () => pm.response.to.have.status(200));
    language: text/javascript
order: 262522056150237
